/* let myValue = Math.max(1, 4, 6, 3, 6, 1, 8, 4, 3, 8, 5, 9);
console.log(myValue);

let my = { z: "first", y: "second" };
Object.assign(
  my,
  { a: "hello", c: "kadiri", f: "rksr" },
  { b: "navi", d: "greke" }
);
console.log(my); */

/********************************************************** */
/* myCal = (a,b,...args) => {
  let mul=a*b;
  let sum = 0;
  for (const arg of args) {
    sum += arg;
  }

  return [sum,mul]
};

console.log(myCal(1,3,4,5,6,3));
 */
/***************************************************************** */
/* myAdditiion=(a,b,c)=>{
  return a+b+c
}
let result=[5,7,3]
console.log(myAdditiion(...result));
 */